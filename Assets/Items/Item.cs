using UnityEngine;

public class Item : MonoBehaviour
{/*
    public bool isHeld = false;      
    public bool isGift = false;    

    private void Start()
    {
        // Register this item with the CentralItemManager
        CentralItemManager.Instance.RegisterItem(this);
    }

    private void OnDestroy()
    {
        if (CentralItemManager.Instance != null)
            CentralItemManager.Instance.UnregisterItem(this);
    }
    */
}
